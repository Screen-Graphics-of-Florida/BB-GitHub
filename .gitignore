# ---------------------------------------------------------------------------
# Never commit - enforces the rules stated in claude-config/README.md
# ---------------------------------------------------------------------------

# Claude Code credentials. Nothing here is sanitized; keep it out permanently.
**/.credentials.json

# Session transcripts - deliberately not synced.
*.jsonl

# ---------------------------------------------------------------------------
# Windows shell junk
# ---------------------------------------------------------------------------
Thumbs.db
ehthumbs.db
desktop.ini

# claude-config lives in its own private repo at C:\Users\Bill\claude-config
# Never commit it here - this repo pushes to the Screen Graphics org.
claude-config/
