%{
*********************************************************************
* Copr 1979 2003 An Unpublished Work By Harris Business Group, Inc. *
* All rights reserved. This work contains trade secrets.            *
*                  					             *
*  Job: Pre-Check Register                                          *
*********************************************************************
%}

%INCLUDE "$(baseVar)"
%INCLUDE "EditRoutines.icl"
%INCLUDE "EdtVar.icl"
%INCLUDE "ErrorMessage.icl"
%INCLUDE "GenericDirectCallVariables.icl"
%INCLUDE "Menu.icl"
%INCLUDE "NewWindowVariables.icl"
%INCLUDE "StoredProcedureVariables.icl"
%INCLUDE "VarBase.icl"

%Define {
  page_title              = "Pre-Check Register"
  d2wName                 = "PreCheckRegister.d2w"
  d2wVarBase              = "$(genericVarBase)&amp;reportSelType=@dtw_rurlescseq(reportSelType)&amp;backHome=@dtw_rurlescseq(backHome)"
  DATABASE                = "*LOCAL"
  DTW_DEFAULT_REPORT      = "NO"
%}

%MACRO_FUNCTION Set_URL (INOUT workURL) {
%}

%MACRO_FUNCTION Set_SQL () {
  %INCLUDE "$(docType)"
  <html>
      <head>
          %INCLUDE "$(headInclude)"
          @dtw_assign(formName, "Chg")
          <script TYPE="text/javascript">
              %INCLUDE "dateedit.icl"
              %INCLUDE "calendar.icl"
              %INCLUDE "Menu.js"
              %INCLUDE "numedit.icl"
              %INCLUDE "NumZero.icl"
              %INCLUDE "CheckEnterChg.icl"
              %INCLUDE "NewWindowOpen.icl"
              function validate(chgForm) {
                  if (document.Chg.checkDate.value =="" ||
                      document.Chg.bank.value =="")
                     {alert("$(reqFieldError)"); return false;}
                  if (editdate(document.Chg.checkDate) &&
                      editZero(document.Chg.bank, 2, 0))
                      return true;
              }
              function returndate9() {
                       document.Chg.schDate.value = '' + padout(month - 0 + 1) + padout(day) + yr2(year);
                       date9win.close();
               }
          </script>
          %INCLUDE "$(genericHead)"
      </head>
      <body $(bodyTagAttr) onKeyPress="checkEnterChg()">
          <!-- Start Of Banner Code -->
          %INCLUDE "banner.icl"
          <!-- End Of Banner Code -->
          <table $(baseTable)>
              <tr valign="top">
                  @dtw_assign(pageID, "PRECHECKREGISTER")
                  <td class="menu">@Menu_Query(profileHandle, dataBaseID, portal, pageID)</td>
                  <td class="content">
                      %INCLUDE "SubmitScheduleTop.icl"
                      %INCLUDE "ConfMessageDisplayNoTable.icl"
                      $(hrTagAttr)
            			
                      <form class="formClass" METHOD=POST NAME="Chg" onSubmit="return validate(document.Chg)" ACTION="$(homeURL)$(cGIPath)$(d2wName)/Edit_Data$(d2wVarBase)&amp;submitSchedule=@dtw_rurlescseq(submitSchedule)">
                          <table $(contentTable)>
                              @dtw_assign(focusField, "checkDate")
                              %if (errFound != "" || scheduleJobSwitch == "Y")
                                  @dtw_assign(scheduleJobSwitch, "")
                                  @EdtVarErr(profileHandle, typeValue, edtVar)
                                  @dtw_assign(focusField, "")
                                  @ErrVarErr(profileHandle, typeError, errVar)
                                  @Decat_Field("@@sbjb")   @dtw_assign(submitSchedule,  fieldValue)
                                  @DecatErr_Field("@@ckdt", "checkDate")   @dtw_assign(Err_N1CKDT,  fieldValue)
                                  @DecatErr_Field("@@bank", "bank")        @dtw_assign(Err_N1BANK,  fieldValue)
                                  %INCLUDE "ScheduleJobErr.icl"    %{ Schedule Entries Errors %}

                                  @Decat_Field("@@ckdt")   @dtw_assign(V_N1CKDT,  fieldValue)
                                  @Decat_Field("@@bank")   @dtw_assign(V_N1BANK,  fieldValue)
                                  %INCLUDE "ScheduleJobValue.icl"    %{ Schedule Entries Values %}
                              %endif

                              @SetTextOvr(Err_N1CKDT)
                              <tr><td class="dsphdr"><span $(textOvr)>Check Date</span></td>
                                  <td class="inputnmbr"><input type="text" name="checkDate" VALUE="$(V_N1CKDT)" size="6" maxlength="6"> $(reqFieldChar)
                                                        <a href="javascript:calWindow('checkDate');">$(calendarImage)</a></td>
                              </tr>
                              @DspErrMsg(Err_N1CKDT)

                              @Default_Zero(V_N1BANK)
                              @RtvFldDesc("BKBANK=$(V_N1BANK)", "HDBANK", "BKBKNM", fieldDesc)
                              @SetTextOvr(Err_N1BANK)
                              <tr><td class="dsphdr"><span $(textOvr)>Bank</span></td>
                                  <td class="inputnmbr"><input type="text" name="bank" value="$(V_N1BANK)" size="6" maxlength="2">
                                                        <a href="$(homeURL)$(phpPath)BankSearch.php$(altVarBase)&amp;docName=Chg&amp;fldName=bank&amp;fldDesc=bankName" onclick="$(searchWinVar)">$(reqFieldChar) $(searchImage)</a>
                                                        <input type="text" disabled name="bankName" value="$(fieldDesc)" size="30"></td>
                              </tr>
                              @DspErrMsg(Err_N1BANK)
                          </table>
                          @dtw_assign(envProgram, "CAPPRE")
                          @dtw_assign(envPrinter, "")
                          %INCLUDE "schedulejob.icl"
                          %INCLUDE "SubmitScheduleBottom.icl"
                          $(hrTagAttr)
                          %INCLUDE "Copyright.icl"

                          %if (focusField !="")
                              <script TYPE="text/javascript">
                                  document.Chg.$(focusField).focus();
                              </script>
                          %endif
                      </form>
                  </td>
              </tr>
          </table>
          <!-- Start Of Trailer Code -->
          %INCLUDE "trailer.icl"
          <!-- End Of Trailer Code -->
      </body>
  </html>
%}

%FUNCTION(dtw_directcall) Validate_Data(INOUT CHAR(64)    profileHandle,
                                        INOUT CHAR(2)     dataBaseID,
                                        INOUT CHAR(1)     submitSchedule,
                                        INOUT CHAR(1)     errFound,
                                        INOUT CHAR(32000) edtVar,                             	
                                        INOUT CHAR(32000) errVar)
{%EXEC {HAPPCS_W.PGM %}
%}

%HTML(REPORT) {
  %INCLUDE "setLibraryList.icl"
  @Set_SQL()
%}

%HTML(Edit_Data) {
  %INCLUDE "setLibraryList.icl"
  @dtw_assign(checkDate,@dtw_rstrip(checkDate))
  @dtw_assign(bank,@dtw_rstrip(bank))

  @dtw_assign(edtVar, "")
  @Concat_Field("@@ckdt", checkDate)
  @Concat_Field("@@bank", bank)
  %INCLUDE "ScheduleJobConcat.icl"    %{ Schedule Entries Values %}
  @dtw_concat(edtVar, "}{", edtVar)

  @Validate_Data(profileHandle, dataBaseID, submitSchedule, errFound, edtVar, errVar)

  %INCLUDE "SubmitScheduleUpdate.icl"
%}